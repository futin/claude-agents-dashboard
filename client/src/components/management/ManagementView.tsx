/**
 * Management section — read-only three-pane view over Claude config
 * (skills/hooks/agents/… per scope). Default export: loaded via React.lazy
 * so the sessions bundle stays unchanged.
 */
export default function ManagementView() {
  return <div className="mgmt-empty">management view — coming in the next commit</div>;
}
